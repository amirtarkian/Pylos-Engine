training:
  selfplay_games: 2000
  search_iterations: 64
  batch_size: 128
  replay_buffer_size: 512
  epochs_per_game: 3
  learning_rate: 0.001
  weight_decay: 0.0001
  c_puct: 1.5
  dirichlet_alpha: 0.3

checkpoints:
  save_every: 100
  eval_games: 50
  dir: engine/checkpoints

wandb:
  enabled: false
  project: pylos-alphazero
